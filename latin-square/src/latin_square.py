"""Check whether a given array is a Latin square."""
import sys


def is_latin_square(size, arr):
    """If it is a Latin square, then display true else, display false."""
    return False


if __name__ == '__main__':
    size = sys.args[0]
    array = sys.args[1]
    print is_latin_square(size, array)
